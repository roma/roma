#!/usr/bin/env ruby
require 'pathname'

base_path = Pathname(__FILE__).dirname.parent.expand_path
$LOAD_PATH.unshift("#{base_path}/lib")

require 'roma/tools/adm'

begin
  adm = Roma::Adm.new(ARGV[0])
  adm.check_type
  adm.send_command
rescue => e
  puts e.message
end

