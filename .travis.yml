language: ruby

sudo: false

cache: bundler

addons:
  apt:
    packages:
      - libtokyocabinet-dev

before_install:
  - gem install bundler

rvm:
  - 2.1.6
  - 2.2.3
  - 2.3.0
  - ruby-head

script: bundle exec ruby test/run-test.rb

matrix:
  allow_failures:
    - rvm: ruby-head
